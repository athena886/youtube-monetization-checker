# YouTube Data API v3 key. Add this as an encrypted Cloudflare Pages secret.
YT_API_KEY=your_youtube_data_api_key

# Bind a Cloudflare KV namespace to this name in production and preview.
# YT_CACHE is a KV binding, not a string environment variable.
